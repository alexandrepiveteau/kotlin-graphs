name: tests
on: [ push, pull_request ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 🎁 Checkout repo
        uses: actions/checkout@v3
      - name: 🐘 Setup Gradle
        uses: gradle/gradle-build-action@v2
      - name: ✅ Run tests
        run: ./gradlew check
